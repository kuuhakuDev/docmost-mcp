services:
  docmost-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docmost-mcp
    restart: unless-stopped
    ports:
      - "${DOCMOST_MCP_PORT:-3001}:3001"
    env_file: .env
    environment:
      - DOCMOST_MCP_TRANSPORT=http
